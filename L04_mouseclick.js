// write your codes here
function setup() {
    createCanvas(600,600);
    background("skyblue");
    noLoop(); // make draw() run once
}

function draw() {
    // print(width);
    // print(height);
    for (let count = 0; count < 6; count++) {
        
    }
}